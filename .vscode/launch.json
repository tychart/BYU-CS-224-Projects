{
    "configurations": [
        {
            
            "name": "Compile and Run With Custom Args",
            "type": "compound",
            "request": "launch",
            // "program": "${fileDirname}/${fileBasenameNoExtension}",
            "preLaunchTask": "Compile the Current C file",

            "postDebugTask": "Run Executable With Custom Args"

        }

    ],
    "version": "2.0.0"
}