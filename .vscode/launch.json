{
    "configurations": [
        {
            
            "name": "Compile and Run With Custom Args",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${fileDirname}/${fileBasenameNoExtension}", 
            "args": [
                "arg1",
                "arg2"
            ]


        }

    ],
    "version": "2.0.0"
}

// 