{
    "version": "2.0.0",
    "configurations": [
        {
            
            "name": "Compile and Run With Custom Args",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "program": "${fileDirname}/${fileBasenameNoExtension}", 
            "args": [
                // "<",
                "${fileDirname}/yo-files/test-nop.yo"
            ],
            "preLaunchTask": "Custom Compile the Current C Project"

        }
    ]    
}

// 